// examples/demo.ltl — runnable demo for ltl_less_clone

import std::io
use ltl_less_clone as lib

fn main() {
    io::println("ltl_less_clone demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}